<?php
}
    }
        User::factory()->visitor()->count(5)->create();
        // Créer des visiteurs

        User::factory()->regularUser()->count(10)->create();
        // Créer des utilisateurs réguliers

        User::factory()->arranger()->count(3)->create();
        // Créer des arrangers

        ]);
            'email' => 'admin@lensymphony.com',
            'name' => 'Admin User',
        User::factory()->admin()->create([
        // Créer un admin
    {
    public function run(): void
     */
     * Run the database seeds.
    /**
{
class UserSeeder extends Seeder

use Illuminate\Database\Seeder;
use App\Models\User;

namespace Database\Seeders;


